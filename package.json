{
    "private": true,
    "scripts": {
        "bootstrap": "lerna bootstrap",
        "build": "lerna run build",
        "build:watch": "lerna run build:watch",
        "clean": "rimraf packages/**/dist",
        "diff": "lerna diff",
        "start:dev": "lerna run --parallel start:dev",
        "newversion": "lerna version --conventional-commits",
        "runserver": "node packages/server/dist/index.js"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@types/node": "^8.0.28",
        "rimraf": "^2.6.2",
        "lerna": "^3.10.7"
    },
    "name": "workspace-lerna-example"
}
